# Order Service environment configuration

# Service port
PORT=3001

# MongoDB connection (points to order-mongo container in docker-compose)
MONGO_URI=mongodb://order-mongo:27017/order_db

# RabbitMQ connection (points to rabbitmq container in docker-compose)
RABBITMQ_URI=amqp://rabbitmq:5672
